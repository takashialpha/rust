[package]
name = "project"
version = "0.1.0"
edition = "2021"

[dependencies]

[profile.release]
strip = true
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
debug = false
overflow-checks = false
incremental = false
